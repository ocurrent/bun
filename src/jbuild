(jbuild_version 1)

(executables
 ((names (bun mon))
  (libraries (bos cmdliner fpath rresult astring spawn afl-persistent)))
)

(install
 ((section bin)
  (files ((bun.exe as bun) (mon.exe as mon)))
))
